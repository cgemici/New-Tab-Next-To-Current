For those who want to be able to create a new tab next to the current tab.

Adds a new keyboard shortcut (default alt+x can be changed from about:addons) so that you can sometimes create a new tab next to the current one without changing the original behavior of ctrl+t.